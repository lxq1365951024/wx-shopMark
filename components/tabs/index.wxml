<view class="tabs">
  <view class="tabs_title">
    <view class="{{item.isActive?'active':''}} title_item" wx:for="{{tabs}}" wx:key='id' bindtap='handleItme' data-index='{{index}}'>
      {{item.title}}
    </view>
  </view>
  <view>
    <slot></slot>
  </view>
</view>
